<h2 id="use-headings-correctly">Use headings correctly</h2>
<p class="nhsuk-body-s">For: <a href="/accessibility/content">Content</a>, <a href="/accessibility/design">Design</a>, <a href="/accessibility/testing">Testing</a></p>
<p>Everyone relies on meaningful headings to navigate the page but they are especially important for people with access needs. Make sure your headings reflect the page structure.</p>
<p>Make sure you have not skipped heading levels, for example from <code>h2</code> to <code>h4</code>. Screen reader users may navigate using a list of headings. You can use a web developer toolbar (<a href="https://chrome.google.com/webstore/detail/web-developer/bfbameneiokkgbdmiekhjnmfkcnldhhm">Chrome</a> or <a href="https://addons.mozilla.org/en-GB/firefox/addon/web-developer/">Firefox</a>) to see the overall heading structure of the page.</p>
<p>Do not use bold text instead of using subheadings. This is inaccessible because a screen reader will not recognise it as a heading.</p>
<p>Find out more about:</p>
<ul>
  <li>styling headings in the <a href="/design-system/styles/typography">typography section</a> of the design system</li>
  <li>"nesting" headings in the content guide - LINK</li>
</ul>

