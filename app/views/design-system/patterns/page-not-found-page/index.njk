{% extends "layouts/app-layout.njk" %}

{% set pageTitle = "Page not found page" %}
{% set pageDescription = "A page not found tells someone we cannot find the page they were trying to view. They are also known as 404 pages." %}
{% set pageSection = "Design system" %}
{% set subSection = "Patterns" %}
{% set theme = "Pages" %}
{% set dateUpdated = "July 2026" %}
{% set backlog_issue_id = "233" %}

{% block beforeContent %}
  {% include "design-system/patterns/_breadcrumb.njk" %}
{% endblock %}

{% block bodyContent %}

{{ designExample({
  group: "patterns",
  item: "page-not-found-page",
  type: "default"
}) }}

<h2 id="when-to-use">When to use this pattern</h2>
<p>Use a page not found if someone is trying to view a page that does not exist. This happens if someone:</p>
<ul>
    <li>selects a link or button that takes them to a page that does not exist</li>
    <li>types or copies a web address for a page that does not exist</li>
    <li>types or copies a web address incorrectly</li>
</ul>
<p>Test all links and buttons to make sure they work. Check that all the web addresses in your service, including letters, are correct.</p>

<h2 id="how-to-use">How to use this pattern</h2>
<p>The page should include:</p>
<ul>
    <li>"Page not found – service name – NHS" as the page title</li>
    <li>"Page not found" as the <code>&lt;h1&gt;</code></li>
    <li>options to navigate back, for example to the start page in a service or the home page in a website</li>
    <li>an alternative journey, for users on a transactional service</li>
    <li>contact information, if it helps meet a user need</li>
</ul>
<p>If your page is part of a healthcare service, tell users how to complete their task through a different channel.</p>
<p>Contact information should include all numbers and opening times or link to a specific page that includes numbers and opening times.</p>

{{ designExample({
  group: "patterns",
  item: "page-not-found-page",
  type: "with-contact-details"
}) }}

<p>If your users find it helpful, include a search option.</p>
<p>Do not ask users to report errors unless you know that this meets a user need.</p>

<p>The content should be clear and concise, not blame the user.</p>

<p>Do not use:</p>
<ul>
    <li>breadcrumbs</li>
    <li>technical jargon like 404 or bad request</li>
    <li>informal or humorous words like "oops"</li>
    <li>red text to warn people</li>
    <li>too many links</li>
</ul>

{% endblock %}
