{% extends "layouts/app-layout.njk" %}

{% set pageTitle = "Service unavailable page" %}
{% set pageDescription = "Tell users what to do when your service is unavailable." %}
{% set pageSection = "Design system" %}
{% set subSection = "Patterns" %}
{% set theme = "Pages" %}
{% set dateUpdated = "July 2026" %}
{% set backlog_issue_id = "588" %}

{% block beforeContent %}
  {% include "design-system/patterns/_breadcrumb.njk" %}
{% endblock %}

{% block bodyContent %}

{{ designExample({
  group: "patterns",
  item: "service-unavailable-page",
  type: "default"
}) }}

<h2 id="when-to-use">When to use this pattern</h2>

<p>Use a service unavailable page when your service cannot be used temporarily. This could be for:</p>
<ul>
  <li>planned maintenance or upgrades</li>
  <li>unplanned problems which will take time to fix</li>
  <li>reasons outside of your control such as a hosting provider outage</li>
</ul>
<p>If there is an unexpected problem and you can fix it quickly, use the <a href="/design-system/patterns/problem-with-the-service-pages">There is a problem with the service pattern</a>. If you cannot fix the problem quickly, use this pattern.</p> 
<p>Have a general page in case you need to make a service unavailable quickly and do not have time to update the page. As soon as you know when the service will be available, update the page.</p>
<p>Consider what users should do while the service is unavailable. For example, include a phone number they can contact for urgent issues or a link to a form they can download to record information temporarily.</p>
<p>You can also use this pattern when a service closes permanently.</p>
<p>There are different versions of this pattern:</p>
<ul>
  <li><a href="#general-page">general page</a></li>
  <li><a href="#when-you-know-when-service-will-be-available">when you know when the service will be available</a></li>
  <li><a href="#updates">when you have updates about availability</a></li>
  <li><a href="#standalone-page">standalone page</a></li>
  <li><a href="#service-closed-permanently">service closed permanently</a></li>
</ul>

<h2 id="how-to-use">How to use this pattern</h2>
<p>The page should include:</p>
<ul> 
  <li>"Sorry, the service is unavailable – service name – NHS" as the page title</li>
  <li>"Sorry, the service is unavailable" as the <code>&lt;h1&gt;</code></li>
  <li>the day, date and time it is going to be available or what to do if it is permanently closed</li>
  <li>information about what has happened to their answers if they are in the middle of a transaction</li>
  <li>an alternative journey, if it's a transactional service</li>
  <li>contact information</li>
</ul>
<p>If your service is a healthcare service, tell users how to complete their task through a different channel.</p>
<p>Contact information should include all numbers and opening times or link to a specific page that includes numbers and opening times. If it's helpful, include "For urgent medical advice" followed by the text "Use <a href="#">111 online</a> or call 111."</p>
<p>Do not ask users to report errors unless you know that this meets a user need.</p>
<p>Have clear and concise content and do not use:</p>
<ul>
  <li>breadcrumbs or a back button</li>
  <li>vague, unhelpful words like maintenance, improvements</li>
  <li>red text to warn people</li>
  <li>too many links</li>
</ul>

<h3 id="general-page">General page</h2>

{{ designExample({
  group: "patterns",
  item: "service-unavailable-page",
  type: "default"
}) }}

<h3 id="when-you-know-when-service-will-be-available">When you know when the service will be available</h2>

<p>Use this if you are planning to make the service unavailable for a short period and can tell people when it will be open again.</p>

{{ designExample({
  group: "patterns",
  item: "service-unavailable-page",
  type: "when-you-know-when-service-will-be-available"
}) }}

<p>If you are planning to make your service unavailable, give users advance warning of this on your service <a href="/design-system/patterns/start-page">start page</a>.</p>
<div class="app-example app-example--content">
  {{ tag({
    text: "Example",
    classes: "app-example__heading"
  }) }}
  <h2 class="nhsuk-heading-s">Service unavailable tonight</h2>
  <p>This service will be temporarily unavailable from 11.59pm on 2 August to 2am on 3 August 2026</p>
</div>

<h3 id="updates">When you have updates about availability</h2>

<p>If the technical issue is unexpected, communicate any updates you have on progress to resolve it.</p>

{{ designExample({
  group: "patterns",
  item: "service-unavailable-page",
  type: "with-update"
}) }}

<h3 id="standalone-page">Standalone page</h2>

<p>If you are using a proxy server or content delivery network (CDN), it will have a error page which is shown automatically whenever your service is unavailable.</p>

<p>As this page is hosted separately, you will need to configure a standalone error page which is not dependent on your service.</p>

<p>Use this standalone template. You will need to edit it to add the name of your service and any relevant content to tell your users what to do if the service is unavailable.</p>

{{ designExample({
  group: "patterns",
  item: "service-unavailable-page",
  type: "standalone",
  htmlOnly: true
}) }}

<h3 id="service-closed-permanently">Service closed permanently</h2>
<p>Refer users to another service or to somewhere that helps them deal with their problem.</p>

{{ designExample({
  group: "patterns",
  item: "service-unavailable-page",
  type: "closed-replaced"
}) }}
<p>Include a note on any pages that refer to the service. [Needs more work.]</p>
{% endblock %}
