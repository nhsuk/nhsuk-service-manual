{% from "header/macro.njk" import header %}

{{ header({
  inline: true,
  search: {
    input: {
      placeholder: "Search"
    },
    label: {
      visuallyHiddenText: "Search the NHS website"
    }
  }
}) }}
