{% set pageTitle = "Table" %}
{% set pageSection = "Design system" %}
{% set subSection = "Components" %}
{% set pageDescription = "Use a table to make it easier for users to compare and scan information." %}
{% set theme = "Content presentation" %}
{% set dateUpdated = "April 2019" %}
{% set backlog_issue_id = "26" %}

{% extends "includes/app-layout.njk" %}

{% block breadcrumb %}
  {% include "design-system/components/_breadcrumb.njk" %}
{% endblock %}

{% block bodyContent %}

  <h2 id="basic-table">Basic table</h2>

  {{ designExample({
    type: "table"
  }) }}

  <h3 id="when-to-use-a-basic-table">When to use a basic table</h3>
  <p>Use a basic table to let users compare information in rows and columns, when your table has 2 columns.</p>

  <h3 id="when-not-to-use-a-basic-table">When not to use a basic table</h3>
  <p>Do not use a basic table:</p>
  <ul>
    <li>to layout content on a page, use the <a href="/design-system/styles/layout#grid">grid system</a> instead</li>
    <li>if you have 3 or more columns, use a <a href="#responsive-table">responsive table</a> instead</li>
  </ul>

  <h2 id="responsive-table">Responsive table</h2>
  <p>On large screens the responsive table looks like a basic table. However, on smaller screens the responsive table collapses.</p>

  {{ designExample({
    type: "table"
  }) }}

  <h3 id="when-to-use-a-responsive-table">When to use a responsive table</h3>
  <p>Use a responsive table to let users compare information in rows and columns, when your table has 3 columns or more.</p>

  <h3 id="when-not-to-use-a-responsive-table">When not to use a responsive table</h3>
  <p>Do not use a responsive table:</p>
  <ul>
    <li>to layout content on a page, use the <a href="/design-system/styles/layout#grid">grid system</a> instead</li>
    <li>if you have 2 columns, use a <a href="#basic-table">basic table</a> instead</li>
  </ul>

  <h2 id="how-it-works">How tables work</h2>
  <h3>Accessibility</h3>
  <p>Follow <a href="https://webaim.org/techniques/tables/data">WebAIM's guidance for tables</a> and</p>
  <ul>
    <li>give tables captions</li>
    <li>use the scope attribute to associate the data cells with the appropriate headers</li>
    <li>let the browser window determine the width of the table whenever possible, to reduce horizontal scrolling</li>
  </ul>

  <h3 id="table-captions">Table captions</h3>
  <p class="rich-text">Use the <code>&lt;caption&gt;</code> element to describe a table in the same way you would use a heading. A caption helps users find, navigate and understand tables.</p>

  <h3 id="table-headers">Table headers</h3>
  <p>Use table headers to tell users what the rows and columns represent.</p>

  <h2 id="reseach">Research</h2>
  <h3>Basic table</h3>
  <p>The basic table tested well with users on health information on the NHS website.</p>

  <h3>Responsive table</h3>

  <h3>Table with panel</h3>
  <p>We have also designed a table with a panel. You can see <a href="https://nhsuk.github.io/nhsuk-frontend/components/tables/tables-panel.html">an example in the frontend library</a>. We haven't included it in this list of components yet because it needs more testing.</p>
  <p>If you’ve used tables, get in touch to <a href="/community">share your user research findings</a>.</p>

{% endblock %}
