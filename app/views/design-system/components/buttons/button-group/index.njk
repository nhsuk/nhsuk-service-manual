{% from "button/macro.njk" import button %}

<div class="nhsuk-button-group">
  {{ button({
    text: "Continue"
  }) }}

  {{ button({
    text: "Save and come back later",
    variant: "secondary"
  }) }}
</div>
