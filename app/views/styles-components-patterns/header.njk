{% set pageTitle = 'Header' %}
{% set pageSection = 'Styles, components and patterns' %}
{% set pageDescription = 'Use the header to show users they are on an NHS service and help them get started in finding what they need.' %}

{% extends 'includes/layout.njk' %}

{% from 'header/macro.njk' import header %}
{% from 'panel/macro.njk' import panel %}
{% from 'includes/design-example.njk' import designExample %}

{% block breadcrumb %}
{{ breadcrumb({
  items: [
    {
      href: "/service-manual/",
      text: "Home"
    }
  ],
  href: "/service-manual/styles-components-patterns",
  text: "Styles, components and patterns"
}) }}
{% endblock %}

{% block body %}
<div class="nhsuk-grid-row">
  <div class="nhsuk-grid-column-full app-component-reading-width">
    <h1>{{ pageTitle }}</h1>
    <p class="nhsuk-lede-text">{{ pageDescription }}</p>

    {{ designExample({
      type: 'header'
    }) }}

    <h2 id="when-to-use-the-header">When to use the header</h2>
    <p>Use the NHS header at the top of every page if your service is on the www.nhs.uk domain.</p>

    <h2 id="when-not-to-use-the-header">When not to use the header</h2>
    <p>Do not use the header if your service is not on the www.nhs.uk domain.</p>
    <p>We have found that the full header is too distracting to be used in transactional services and we recommend using the transactional header instead.</p>

    <h2 id="how-to-use">How to use</h2>

    <h3 id="default-header">Default header</h3>
    <p>Use the default header on the NHS website.</p>

    {{ designExample({
      type: 'header'
    }) }}

    <h3 id="service-header">Service header</h3>
    <p>Use the service header if you are a service on the NHS website.</p>

    {{ designExample({
      type: 'header-service'
    }) }}

    <h3 id="transactional-header">Transactional header</h3>
    <p>Use the transactional header for transactional journeys on the NHS website, service names can be used to help users understand which service they are using.</p>

    {{ designExample({
      type: 'header-transactional'
    }) }}

    <h3 id="organisational-header">Organisational header (experimental)</h3>
    <p>Use the organisational header if you are an NHS organisation, such as a NHS Clinical Commissioning Group or NHS Trust, that has a organisational logo.</p>
    <p>You must have a Frutiger font licence to use an organisational logo. See the <a href="https://www.england.nhs.uk/nhsidentity/identity-guidelines/organisational-logos/">NHS England identity guidelines - Organisational logos</a> for more information on how to use organisational logos.</p>

    <p>[Design example]</p>

    <h4>White variant</h4>

    <p>[Design example]</p>

    <p>The organisational logo is an SVG and you can change the organisation name and descriptor within code. Longer organisation names should be split onto 2 lines.</p>
    <p>You can also use a static asset, such as a PNG file.</p>

<pre class="language-markup"><code class="language-markup"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>a</span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>nhsuk-header__link<span class="token punctuation">"</span></span> <span class="token attr-name">href</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>/<span class="token punctuation">"</span></span> <span class="token attr-name">aria-label</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>Anyplace Anytown Anywhere NHS Foundation Trust homepage<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>img</span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>nhsuk-org-logo<span class="token punctuation">"</span></span> <span class="token attr-name">src</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>/path-to-assets/logo.png<span class="token punctuation">"</span></span> <span class="token attr-name">alt</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span><span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>a</span><span class="token punctuation">&gt;</span></span></code></pre>

    <p>The organistional header is an experimental component, we haven't tested this in a real service, so we are looking for <a href="https://github.com/nhsuk/nhsuk-service-manual-backlog/issues/156">feedback on the organisational header on GitHub</a>.</p>

    <h2 id="discuss">Discuss</h2>
    <p><a href="https://github.com/nhsuk/nhsuk-service-manual-backlog/issues/16">Discuss the header on GitHub</a></p>

    <h2 id="get-in-touch">Get in touch</h2>
    <p>If you have a question:</p>
    <ul>
      <li>join the <a href="https://join.slack.com/t/nhs-service-manual/shared_invite/enQtNTIyOTEyNjU3NDkyLTk4NDQ3YzkwYzk1Njk5YjAxYTI5YTVkZmUxMGQ0ZjA3NjMyM2ZkNjBlMWMxODVjZjYzNzg1ZmU4MWY1NmE2YzE">NHS.UK service manual Slack workspace</a></li>
      <li>email <a href="mailto:service-manual@nhs.net">service-manual@nhs.net</a></li>
    </ul>

    <div class="nhsuk-review-date">
     <p class="nhsuk-body-s">Updated: October 2019</p>
    </div>

    {{ panel({
    "label": "Similar components",
    "HTML": "<ul>
              <li><a href='/service-manual/styles-components-patterns/footer'>Footer</a></li>
            </ul>"
    }) }}
  </div>
</div>
{% endblock %}
